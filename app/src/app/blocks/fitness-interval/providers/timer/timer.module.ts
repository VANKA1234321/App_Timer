import	{ NgModule } from '@angular/core';

import	{ IonicModule } from '@ionic/angular';

import	{ TimerProvider } from './timer';

@NgModule({
	imports: [

	],
	providers: [
		TimerProvider
	]
})
export	class TimerProviderModule { }
